FIND_FILES()
IF ( USE_DEVICE )
    ADD_PACKAGE_SUBDIRECTORY( device )
ELSEIF( USE_KOKKOS OR USE_TRILINOS_KOKKOS )
    ADD_PACKAGE_SUBDIRECTORY( kokkos )
ELSEIF( USE_OPENMP )
    ADD_PACKAGE_SUBDIRECTORY( openmp )
ELSE()
    ADD_PACKAGE_SUBDIRECTORY( serial )
ENDIF()

SET( HEADERS ${HEADERS} PARENT_SCOPE )
SET( CXXSOURCES ${CXXSOURCES} PARENT_SCOPE )
SET( CUDASOURCES ${CUDASOURCES} PARENT_SCOPE )
SET( HIPSOURCES ${HIPSOURCES} PARENT_SCOPE )
SET( CSOURCES ${CSOURCES} PARENT_SCOPE )
SET( FSOURCES ${FSOURCES} PARENT_SCOPE )
SET( SOURCES ${SOURCES} PARENT_SCOPE )

